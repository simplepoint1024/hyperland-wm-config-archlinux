#!/usr/bin/env bash
set -euo pipefail
for cmd in thunar dolphin nautilus pcmanfm; do
  if command -v "$cmd" >/dev/null 2>&1; then
    if [ "${1-}" = "--print" ]; then
      echo "$cmd"
    else
      exec "$cmd"
    fi
    exit 0
  fi
done
echo 'No file manager found.' >&2
exit 1
